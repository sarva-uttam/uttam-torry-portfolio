# source duplicate of assets/frames — not needed for the site
Video frames/

# local tooling
.semgrep/
.playwright-mcp/
.vscode/

# verification screenshots (root-level only; assets/ pngs are kept)
/*.png
/*.local.*

# OS junk
.DS_Store
Thumbs.db
desktop.ini
